# ── QuantCore Pro Environment ────────────────────────────────────────────────
# Copy this file to .env.local for local overrides (git-ignored by default).

# Development server port — used by both Vite and Electron dev mode
VITE_DEV_PORT=5173

# Gemini AI API key (optional)
# VITE_GEMINI_API_KEY=
